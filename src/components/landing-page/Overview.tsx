import { SectionHeader } from "../global/SectionHeader"
import { SectionWrapper } from "../global/SectionWrapper"

export const Overview = () => {
    return <SectionWrapper className="items-center">
        <SectionHeader>
            Overview
        </SectionHeader>
    </SectionWrapper>
}